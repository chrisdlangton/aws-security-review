---
rules:
  langton:
    - do_not_setup_access_keys_during_initial_user_setup_for_all_iam_users_that_have_a_console_password
findings:
  langton:
    - id: 7face6eb
      rule: config_recording
      reason: testing
      ignore_until: '2019-05-02'
